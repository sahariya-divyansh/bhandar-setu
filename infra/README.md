# Bhandar Setu — Infrastructure & Deployment

Infrastructure configuration for serverless deployment on Google Cloud Platform.

- `cloudrun/`: Dockerfile and service manifest for Cloud Run (Backend FastAPI API)
- `firebase/`: Hosting configuration for Firebase (Frontend React SPA)
