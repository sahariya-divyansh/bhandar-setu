[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bhandar-setu-backend"
version = "0.1.0"
description = "FastAPI backend service for Bhandar Setu medicine stock-out prediction and redistribution platform"
authors = [
    { name = "Bhandar Setu Engineering Team" }
]
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "pydantic>=2.6.0",
    "python-dotenv>=1.0.1",
    "google-generativeai>=0.4.0",
    "scikit-learn>=1.4.0",
    "pandas>=2.2.0"
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "httpx>=0.27.0",
    "black>=24.2.0",
    "flake8>=7.0.0"
]
